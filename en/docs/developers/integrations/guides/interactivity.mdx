---
title: Update text with a button
description: Build a block where a button updates displayed text.
metaTitle: Update text with a button | Developers | GitBook Documentation
ogImage: https://test-dai.imgix.net/org-4afef969-21f9-40f3-b5b8-b06c8b6cf68f/doc-22a3b060-9539-4176-97e4-49120e02f5d1/0a5a1a9630785855-organizations-d8f63b60-89ae-11e7-8574-5927d48c4877-sites-site_p4Xo4-socialprevie.jpg?fm=auto&auto=compress%2Cformat
---

This guide builds a custom block with a button. Selecting the button updates text in the block.

### Before you start

You need a local integration project. Follow the [integration quickstart](/en/docs/developers/integrations/quickstart) before continuing.

### Add the component

In your integration entry file, create and register the component:

```tsx
import { createComponent, createIntegration } from '@gitbook/runtime';

const textUpdater = createComponent({
    componentId: 'text-updater',
    initialState: () => ({
        message: 'Select the button to update this text.'
    }),
    async action(element, action) {
        if (action.action !== 'update-message') {
            return {};
        }

        return {
            state: {
                message: 'The button updated this text.'
            }
        };
    },
    async render(element) {
        return (
            <block>
                <text>{element.state.message}</text>
                <button
                    label="Update text"
                    onPress={{
                        action: 'update-message'
                    }}
                />
            </block>
        );
    }
});

export default createIntegration({
    components: [textUpdater]
});
```

The button dispatches the `update-message` action. The action handler returns the new component state.

### Add the block to your manifest

In `gitbook-manifest.yaml`, add the block to the `blocks` list:

```yaml
title: My integration
blocks:
  - id: text-updater
    title: Text updater
```

The manifest block ID must match `componentId`.

### Test the block

1. Start your integration with the development command.
2. Insert **Text updater** from the inline palette.
3. Select **Update text**.

The text changes after the integration handles the action.

### Continue building

Choose a focused guide for the next interaction:

- [Create an interactive text input](/en/docs/developers/integrations/guides/create-an-interactive-text-input).
- [Send data to a webframe](/en/docs/developers/integrations/guides/send-data-to-a-webframe).
- [Open a modal from a button](/en/docs/developers/integrations/guides/open-a-modal-from-a-button).
- [Save editable block content](/en/docs/developers/integrations/guides/save-editable-block-content).

For component options and supported actions, see the component reference.
