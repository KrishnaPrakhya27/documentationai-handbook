---
title: List all GitBook Agent channels for a site
metaTitle: List all GitBook Agent channels for a site | Developers | GitBook Documentation
ogImage: https://test-dai.imgix.net/org-4afef969-21f9-40f3-b5b8-b06c8b6cf68f/doc-22a3b060-9539-4176-97e4-49120e02f5d1/0a5a1a9630785855-organizations-d8f63b60-89ae-11e7-8574-5927d48c4877-sites-site_p4Xo4-socialprevie.jpg?fm=auto&auto=compress%2Cformat
---

Returns a paginated list of channels configured for the site. Channels are integrations that allow GitBook Agent to operate within external tools your team already uses, bringing documentation assistance directly into existing workflows. Each channel has a role that determines whether the Agent answers questions from the site's documentation or actively collaborates by opening and managing content changes. Use this to inspect or manage the Agent channel integrations for a site.

**GET** `/orgs/{organizationId}/sites/{siteId}/channels`

**Parameters**

<ParamField header="Authorization" param-type="string" />

<ParamField path="organizationId" param-type="string" required={true}>
  The unique id of the organization
</ParamField>

<ParamField path="siteId" param-type="string" required={true}>
  The unique id of the site
</ParamField>

<ParamField query="page" param-type="string">
  Identifier of the page results to fetch.
</ParamField>

<ParamField query="limit" param-type="number">
  The number of results per page
</ParamField>

**Responses**

`200` OK

<ResponseField name="next" field-type="object">
  <Expandable title="Object Properties">
    <ResponseField name="page" field-type="string" required={true}>
      Unique identifier to query the next results page
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="count" field-type="number">
  Total count of objects in the list
</ResponseField>

<ResponseField name="items" field-type="array" required={true}>
  <Expandable title="Array Items">
    <ResponseField name="object" field-type="string" required={true} enum="site-channel" />

    <ResponseField name="id" field-type="string" required={true}>
      Unique identifier for the site channel.
    </ResponseField>

    <ResponseField name="status" field-type="string" required={true} enum="active,pending" />

    <ResponseField name="role" field-type="string" required={true} enum="collaborator,support" />

    <ResponseField name="answers" field-type="integer" required={true}>
      Number of answers linked to this channel.
    </ResponseField>

    <ResponseField name="createdAt" field-type="string" required={true} />

    <ResponseField name="urls" field-type="object" required={true}>
      <Expandable title="Object Properties">
        <ResponseField name="location" field-type="string" required={true}>
          URL of the site channel in the API.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
