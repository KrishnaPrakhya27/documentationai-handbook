---
title: List all MCP servers configured for a site
metaTitle: List all MCP servers configured for a site | Developers | GitBook Documentation
ogImage: https://test-dai.imgix.net/org-4afef969-21f9-40f3-b5b8-b06c8b6cf68f/doc-22a3b060-9539-4176-97e4-49120e02f5d1/0a5a1a9630785855-organizations-d8f63b60-89ae-11e7-8574-5927d48c4877-sites-site_p4Xo4-socialprevie.jpg?fm=auto&auto=compress%2Cformat
---

Returns a paginated list of Model Context Protocol (MCP) server configurations attached to the site. MCP servers expose tools and resources that the AI Assistant can call when helping site visitors find information. Use this to audit or manage the AI tool integrations available on a site.

**GET** `/orgs/{organizationId}/sites/{siteId}/mcp-servers`

**Parameters**

<ParamField header="Authorization" param-type="string" />

<ParamField path="organizationId" param-type="string" required={true}>
  The unique id of the organization
</ParamField>

<ParamField path="siteId" param-type="string" required={true}>
  The unique id of the site
</ParamField>

<ParamField query="page" param-type="string">
  Identifier of the page results to fetch.
</ParamField>

<ParamField query="limit" param-type="number">
  The number of results per page
</ParamField>

**Responses**

`200` OK

<ResponseField name="next" field-type="object">
  <Expandable title="Object Properties">
    <ResponseField name="page" field-type="string" required={true}>
      Unique identifier to query the next results page
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="count" field-type="number">
  Total count of objects in the list
</ResponseField>

<ResponseField name="items" field-type="array" required={true}>
  <Expandable title="Array Items">
    <ResponseField name="object" field-type="string" required={true} enum="site-mcp-server" />

    <ResponseField name="id" field-type="string" required={true}>
      Unique identifier for the MCP server
    </ResponseField>

    <ResponseField name="name" field-type="string" required={true}>
      Name of the MCP server
    </ResponseField>

    <ResponseField name="url" field-type="string" required={true} />

    <ResponseField name="transport" field-type="string" required={true} enum="sse,http">
      Transport protocol used to connect to the MCP server
    </ResponseField>

    <ResponseField name="headers" field-type="object" required={true}>
      HTTP headers sent with requests to this server
    </ResponseField>

    <ResponseField name="condition" field-type="string">
      Expression to evaluate
    </ResponseField>

    <ResponseField name="urls" field-type="object" required={true}>
      URLs associated with the object

      <Expandable title="Object Properties">
        <ResponseField name="location" field-type="string" required={true}>
          URL of the MCP server in the API
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
